import { ChannalContainer } from './StyledComponents';

const Channel = () => {
    return (
        <ChannalContainer>
            <h1> Welcome TO Channel Dashboard </h1>
        </ChannalContainer>
    );
};

export default Channel;
