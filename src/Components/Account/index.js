import { AccountContainer } from './StyledComponents';

const Account = () => {
    return (
        <AccountContainer>
            <h1>Hi, welcome to the Account</h1>
        </AccountContainer>
    );
};

export default Account;
