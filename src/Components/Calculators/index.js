import { CalContainer } from './StyledComponents';

const Calculators = () => {
    return (
        <CalContainer>
            <h1>Wellcome to Calculators Dashboard</h1>
        </CalContainer>
    );
};

export default Calculators;
